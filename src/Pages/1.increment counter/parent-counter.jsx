import { useState } from "react";
import { Link } from "react-router-dom";
// import Increment from "./increment";
// import Decrement from "./Decrement";
// import Multiply from "./multiple";

const ParentCounter = () => {
//   const [value, setValue] = useState(0);

//   const increment = () => {
//     setValue((prev) => prev + 1);
//   };

//   const decrement = () => {
//     setValue((prev) => prev - 1);
//   };

//   const multiply = () => {
//     setValue((prev) => prev * 2);
//   };

  return (
    <div>
      {/* <Increment valIncrement={increment} myCounter={value} /> */}
      {/* <Decrement valDecrement={decrement} myCounter={value} /> */}
      {/* <Multiply valMultiply={multiply} myCounter={value} /> */}
    </div>
  );
};

export default ParentCounter;
